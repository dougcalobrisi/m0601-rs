# The workspace denies unwrap/expect/panic (an unhandled panic in the drive
# loop means an uncontrolled motor) — but tests may assert freely.
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests = true
